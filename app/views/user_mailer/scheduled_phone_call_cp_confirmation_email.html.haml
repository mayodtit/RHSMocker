%p
  #{@scheduled_phone_call.user.first_name} booked a Welcome Call with you
  at #{@scheduled_phone_call.scheduled_at.strftime('%m-%d-%Y %H:%M')} for
  #{@scheduled_phone_call.scheduled_duration/60} minutes.
%ol
  %li Add the attached ICS to your Better calendar.
  %li Remove the placeholder event at the same time.
%p
  You will be notified by email if the member cancels or reschedules through the app or another PHA.
