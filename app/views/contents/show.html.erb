<p id="notice"><%= notice %></p>

<% if !@content.content_authors.first.nil? %>
		<% if !@content.content_authors.first.user.imageURL.empty? %>
			<div class="authorPicture" style="float:left">
				<%= image_tag @content.content_authors.first.user.imageURL %>
			</div>
		<% end %>
		<div class="content_subtitle"> 
			<%= @content.created_at.to_s %>&nbsp;|&nbsp;By&nbsp;<%= @content.content_authors.first.user.firstName %>&nbsp;<%= @content.content_authors.first.user.lastName %>
		</div> 
		<div class="content_text">
			<%= @content.text %>
		</div>
<% end %>


<%= link_to 'Edit', edit_content_path(@content) %> |
<%= link_to 'Back', contents_path %>
